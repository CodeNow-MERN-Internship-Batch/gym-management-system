# gym-management-system
A full-stack Gym Management Web App built with React, Node.js, Express &amp; MongoDB. Features a responsive landing page, JWT authentication, separate Admin &amp; User dashboards with full CRUD operations for members, trainers, membership plans and services.
